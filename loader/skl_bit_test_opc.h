OPC(BT)                         /* Bit test (reg, reg, reg). */
OPC(BTI)                        /* Bit test (reg, immed, reg). */

OPC(BTM)                        /* Bit test (reg, memory, reg). */
OPC(BTMI)                       /* Bit test (immed, memory, reg). */

OPC(BTMC)                       /* Bit clear (reg, reg, reg).  */
OPC(BTMCI)                      /* Bit clear (immed, memory, reg). */

OPC(BTMS)                       /* Bit set (reg, memory, reg). */
OPC(BTMSI)                      /* Bit set (immed, memory, reg). */
